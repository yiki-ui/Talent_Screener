{
  "name": "umurava-recruiter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Run separately: cd backend && npm run dev and cd frontend && npm run dev'",
    "build": "echo 'Run separately: cd backend && npm run build and cd frontend && npm run build'"
  }
}
